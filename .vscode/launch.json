{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run ts_forecasting.env_info (uses src PYTHONPATH)",
      "type": "python",
      "request": "launch",
      "module": "ts_forecasting.env_info",
      "justMyCode": true,
      "envFile": "${workspaceFolder}/.env",
      "env": {
        "PYTHONPATH": "${workspaceFolder}/src"
      }
    }
  ]
}
